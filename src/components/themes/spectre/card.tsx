import * as React from "react";

// import "css/spectre.min.css";

const Card = (props: any) => <div className="card" {...props} />;
const Block = (props: any) => <div className="card-body" {...props}/>;

export { Card, Block };